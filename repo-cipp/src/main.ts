const app = document.getElementById('app')
if (app) {
  app.innerHTML = `
    <main style="font-family: system-ui, Arial; padding: 2rem;">
      <h1>CIPP AI — Minimal Build</h1>
      <p>This is a minimal placeholder so Vercel's `vite build` produces an output.</p>
    </main>
  `
}
